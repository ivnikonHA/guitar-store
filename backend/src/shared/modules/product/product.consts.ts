export const enum ProductNameLength {
  Min = 10,
  Max = 100
};

export const enum ProductDecriptionLength {
  Min = 20,
  Max = 1024
};

export const enum ProductArticleLength {
  Min = 5,
  Max = 40
};

export const enum ProductPrice {
  Min = 100,
  Max = 1000000
};

export const enum SortType {
  Up = 1,
  Down = -1
};
